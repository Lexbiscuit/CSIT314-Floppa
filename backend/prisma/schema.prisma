// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Bids {
  bidId      Int     @id @default(autoincrement()) @db.Integer
  accountId  Int     @db.Integer
  workslotId Int     @db.Integer
  status     String  @db.Text
  reason     String? @db.Text

  account  Accounts  @relation(fields: [accountId], references: [accountId])
  workslot Workslots @relation(fields: [workslotId], references: [workslotId])

  @@map("Bids")
}

model Accounts {
  accountId Int      @id @default(autoincrement()) @db.Integer
  profileId Int      @db.Integer
  name      String   @db.Text
  email     String   @unique @db.Text
  password  String   @db.Text
  dob       DateTime @db.Date
  role      String?  @db.Text

  profile Profiles @relation(fields: [profileId], references: [profileId])
  bids    Bids[]

  @@map("Accounts")
}

model Profiles {
  profileId   Int    @id @default(autoincrement()) @db.Integer
  name        String @unique @db.Text
  description String @db.Text

  accounts Accounts[]

  @@map("Profiles")
}

model Workslots {
  workslotId Int      @id @default(autoincrement()) @db.Integer
  startTime  DateTime @db.Timestamp
  endTime    DateTime @db.Timestamp

  Bids            Bids[]
  Workslots_Roles Workslots_Roles[]

  @@map("Workslots")
}

model Workslots_Roles {
  workslotRoleId Int    @id @default(autoincrement()) @db.Integer
  workslotId     Int    @db.Integer
  role           String @db.Text
  count          Int    @db.Integer

  workslot Workslots @relation(fields: [workslotId], references: [workslotId])

  @@map("Workslots_Roles")
}

// model Suspendedaccounts {
// suspendedaccountId  Int @id @default(autoincrement()) @db.Integer
// accountId Int @db.Integer

// }

// model Suspendedprofiles {
// suspendedprofileId  Int @id @default(autoincrement()) @db.Integer

// }
